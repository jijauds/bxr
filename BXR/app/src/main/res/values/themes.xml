<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BXR" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="SelectorButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:background">@drawable/rounded_card</item>
        <item name="android:textColor">@color/light_gray</item>
        <item name="android:backgroundTint">@color/accent</item>
        <item name="android:fontFamily">@font/jersey</item>
    </style>

    <style name="Theme.BXR" parent="Base.Theme.BXR" />
</resources>